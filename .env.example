# Copy to .env and set your keys. The app loads .env automatically when you run the CLI (python -m src.run).
# LLM API key — required for Judges (and optional Vision). Set one of:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...

# LangSmith tracing (optional)
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=

# Vision model (optional at runtime; required for VisionInspector when enabled)
# GEMINI_API_KEY=...
# OPENAI_API_KEY=...  # if using GPT-4o for vision
